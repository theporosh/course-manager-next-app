/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,

  images: {
    domains: ['i.ibb.co.com'],
  },
  images: {
    domains: ['images.unsplash.com'],
  },
};

export default nextConfig;
